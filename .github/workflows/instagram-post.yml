name: Instagram Automated Poster

concurrency:
  group: azvscars-instagram-publish
  cancel-in-progress: false

on:
  schedule:
    # Monday 15:10 AZT -> 11:10 UTC
    - cron: "10 11 * * 1"
    # Tuesday 13:20 AZT -> 09:20 UTC
    - cron: "20 9 * * 2"
    # Tuesday 19:10 AZT -> 15:10 UTC
    - cron: "10 15 * * 2"
    # Wednesday 12:40 AZT -> 08:40 UTC
    - cron: "40 8 * * 3"
    # Wednesday 23:00 AZT -> 19:00 UTC
    - cron: "0 19 * * 3"
    # Thursday 12:20 AZT -> 08:20 UTC
    - cron: "20 8 * * 4"
    # Thursday 20:30 AZT -> 16:30 UTC
    - cron: "30 16 * * 4"
    # Friday 13:30 AZT -> 09:30 UTC
    - cron: "30 9 * * 5"
    # Friday 22:30 AZT -> 18:30 UTC
    - cron: "30 18 * * 5"

  workflow_dispatch:
    inputs:
      type:
        description: 'Post Type to generate'
        required: true
        default: 'main'
        type: choice
        options:
          - quick
          - main
          - war
          - night
          - cinematic
          - market
          - night_supercar

jobs:
  post-to-instagram:
    runs-on: ubuntu-latest
    
    env:
      ADMIN_PASS: ${{ secrets.ADMIN_PASS }}
      PAGES_BASE_URL: ${{ secrets.PAGES_BASE_URL }}
      INGEST_VIA_PAGES: "true"
      PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
      PIXABAY_API_KEY: ${{ secrets.PIXABAY_API_KEY }}
      FREESOUND_API_KEY: ${{ secrets.FREESOUND_API_KEY }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v5

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: 'pip'

      - name: Determine Post Type
        id: set-type
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "POST_TYPE=${{ github.event.inputs.type }}" >> "$GITHUB_ENV"
            echo "POSTING_SLOT=manual" >> "$GITHUB_ENV"
          else
            if [[ "${{ github.event.schedule }}" == "10 11 * * 1" ]]; then
              echo "POST_TYPE=quick" >> "$GITHUB_ENV"
              echo "POSTING_SLOT=mon_1510_buying" >> "$GITHUB_ENV"
            elif [[ "${{ github.event.schedule }}" == "20 9 * * 2" ]]; then
              echo "POST_TYPE=market" >> "$GITHUB_ENV"
              echo "POSTING_SLOT=tue_1320_market" >> "$GITHUB_ENV"
            elif [[ "${{ github.event.schedule }}" == "10 15 * * 2" ]]; then
              echo "POST_TYPE=war" >> "$GITHUB_ENV"
              echo "POSTING_SLOT=tue_1910_debate" >> "$GITHUB_ENV"
            elif [[ "${{ github.event.schedule }}" == "40 8 * * 3" ]]; then
              echo "POST_TYPE=main" >> "$GITHUB_ENV"
              echo "POSTING_SLOT=wed_1240_main" >> "$GITHUB_ENV"
            elif [[ "${{ github.event.schedule }}" == "0 19 * * 3" ]]; then
              echo "POST_TYPE=night_supercar" >> "$GITHUB_ENV"
              echo "POSTING_SLOT=wed_2300_supercar" >> "$GITHUB_ENV"
            elif [[ "${{ github.event.schedule }}" == "20 8 * * 4" ]]; then
              echo "POST_TYPE=market" >> "$GITHUB_ENV"
              echo "POSTING_SLOT=thu_1220_market" >> "$GITHUB_ENV"
            elif [[ "${{ github.event.schedule }}" == "30 16 * * 4" ]]; then
              echo "POST_TYPE=night_supercar" >> "$GITHUB_ENV"
              echo "POSTING_SLOT=thu_2030_supercar" >> "$GITHUB_ENV"
            elif [[ "${{ github.event.schedule }}" == "30 9 * * 5" ]]; then
              echo "POST_TYPE=war" >> "$GITHUB_ENV"
              echo "POSTING_SLOT=fri_1330_war" >> "$GITHUB_ENV"
            elif [[ "${{ github.event.schedule }}" == "30 18 * * 5" ]]; then
              echo "POST_TYPE=night_supercar" >> "$GITHUB_ENV"
              echo "POSTING_SLOT=fri_2230_supercar" >> "$GITHUB_ENV"
            else
              echo "POST_TYPE=main" >> "$GITHUB_ENV"
              echo "POSTING_SLOT=manual" >> "$GITHUB_ENV"
            fi
          fi

      - name: Humanize Scheduled Time
        if: github.event_name == 'schedule'
        run: |
          seed="$(TZ=Asia/Baku date +%Y%m%d)-${{ github.event.schedule }}"
          jitter=$(( 180 + ( $(printf "%s" "$seed" | cksum | awk '{print $1}') % 1320 ) ))
          echo "Natural schedule jitter: ${jitter}s"
          sleep "$jitter"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Automated Post
        timeout-minutes: 12
        run: |
          SID="auto$(date +%Y%m%d%H%M%S)"
          MAKE_REEL="false"
          if [[ "$POST_TYPE" == "quick" || "$POST_TYPE" == "war" || "$POST_TYPE" == "night" || "$POST_TYPE" == "cinematic" || "$POST_TYPE" == "market" || "$POST_TYPE" == "night_supercar" ]]; then
            MAKE_REEL="true"
          fi
          python worker.py \
            --action generate \
            --sid "$SID" \
            --post_type "$POST_TYPE" \
            --make_reel "$MAKE_REEL" \
            --auto_publish true
